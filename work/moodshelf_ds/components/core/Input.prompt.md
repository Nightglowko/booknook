Inset paper text field; the border warms to candlelight gold on focus.

```jsx
<Input variant="search" placeholder="Search your library…" />
<Input icon="feather" placeholder="Add a margin note" />
```
