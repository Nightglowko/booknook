Soft warm surface plate. The base container for shelves, book rows, stats and prompts.

```jsx
<Card tone="paper" pad="md">…</Card>
<Card tone="inverse" pad="lg">Dark moody panel</Card>
<Card interactive onClick={open}>Tappable</Card>
```

Tones: `paper` `raised` `sunken` `inverse`. `interactive` adds a hover lift.
