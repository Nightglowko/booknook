Reading-status pill with a colored leading dot.

```jsx
<Badge status="reading" />
<Badge status="finished" />
<Badge status="favorite">Ruined me</Badge>
```

Statuses: `reading` `finished` `want` `dnf` `favorite`. Children override the default label.
