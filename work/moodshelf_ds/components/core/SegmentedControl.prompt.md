Pill segmented control for view and filter switches; the active segment carries the candlelight wash.

```jsx
<SegmentedControl
  options={['Reading', 'Finished', 'Want']}
  value={tab}
  onChange={setTab}
/>
```
